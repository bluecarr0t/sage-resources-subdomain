/** Primary Sage outdoor-hospitality data table (properties, sites, units). */
export const ALL_SAGE_DATA_TABLE = 'all_sage_data' as const;

/** Admin list view: one anchor row per logical property. */
export const ALL_SAGE_DATA_LIST_ANCHORS_VIEW =
  'all_sage_data_list_anchors' as const;

/** `source` value for Sage rows in unified comps / sites export. */
export const ALL_SAGE_DATA_COMP_SOURCE = 'all_sage_data' as const;
