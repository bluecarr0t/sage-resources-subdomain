/**
 * Limits for POST /api/admin/comps-v2/gap-fill `existingCandidates` (dedupe payload rows).
 * Keep in sync with admin UI pre-checks.
 */
export const COMPS_V2_GAP_FILL_MAX_EXISTING_CANDIDATES = 2500;
