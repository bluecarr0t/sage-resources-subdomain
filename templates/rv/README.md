# RV Report Templates

Place the RV report templates here before running `npx tsx scripts/upload-report-templates.ts`:

- **template.docx** – Convert `OLDER RV TEMPLATE 10-25-25.doc` to .docx (Word/LibreOffice: Save As → .docx). Add Docxtemplater placeholders if needed: `{property_name}`, `{location}`, `{client_entity}`, `{report_date}`, `{executive_summary}`
- **template.xlsx** – Copy `OLDER RV TEMPLATE 03-01-26.xlsx` here
